/**
 * Created by Arnab Karmakar on 9/5/17.
 */
//import { changeText, CHANGE_TEXT } from '../other';
//import {expect} from 'chai';

describe('Test other.js', () => {
  const text = 'test';
  const expected = {
    //type: CHANGE_TEXT,
    //text: text,
  };
  //expect(changeText(text)).to.be.equal(expected);
});
